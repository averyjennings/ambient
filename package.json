{
  "name": "ambient",
  "version": "0.1.0",
  "description": "An agentic shell layer that makes any coding agent ambient and context-aware",
  "type": "module",
  "bin": {
    "r": "./dist/cli/index.js",
    "ambient": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start:daemon": "node dist/daemon/index.js",
    "reload": "tsc && node dist/cli/index.js daemon stop 2>/dev/null; node dist/cli/index.js daemon start && echo '✓ ambient reloaded. Run: source shell/ambient.zsh (only needed if shell hooks changed)'",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "private": true,
  "license": "MIT"
}
